importFrom("utils", "read.csv", "write.csv", "write.table", "capture.output", "installed.packages", "packageDescription", "remove.packages")
importFrom("stats", "na.omit", "dist", "relevel")
importFrom("methods", "is")

useDynLib(admisc, .registration = TRUE)

export(
    alteb,
    agteb,
    aeqb,
    asNumeric,
        curlyBrackets,
        insideBrackets,
        outsideBrackets,
        roundBrackets,
        squareBrackets,
        expandBrackets,
    checkMV,
    checkSubset,
    classify,
    combnk,
    compute,
    dashes,
    expand,
    export,
    factorize,
    finvert,
    frelevel,
    getInfo,
    getLevels,
    getMatrix,
    intersection,
    invert,
    isElement,
    list.rda,
    permutations,
    possibleNumeric,
    prettyString,
    prettyTable,
    deMorgan,
    obj.rda,
    negate,
    recode,
        hastilde,
        notilde,
        tildae,
        tilde1st,
    recreate,
    reload,
    replaceText,
    simplify,
    sop,
    splitstr,
    sortExpressions,
    translate,
    trimstr,
    tryCatchWEM,
    uninstall,
    unload,
    unlockEnvironment,
    validateNames,
    verify,
    wholeNumeric,
    writePrimeimp,
    
        splitMainComponents,
        splitBrackets,
        removeSingleStars,
        splitPluses,
        splitStars,
        splitTildas,
        solveBrackets,
        simplifyList,
        getNonChars,

    has_tag,
    get_tag,
    tag_na,
    is_tagged_string
)


S3method(print, "admisc_factorize")
S3method(print, "admisc_intersection")
S3method(print, "admisc_translate")
S3method(print, "admisc_deMorgan")
S3method(print, "admisc_simplify")
